services:
  postgres:
    image: postgres:18
    container_name: calendar-postgres

    environment:
      POSTGRES_DB: calendar_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5439:5432"

    volumes:
      - calendar_postgres_data:/var/lib/postgresql

volumes:
  calendar_postgres_data: